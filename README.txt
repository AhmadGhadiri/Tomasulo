To compile the program execute the below command:
$ javac -d bin -sourcepath src  src/Tomasulo.java
After compilation to execute the program, run the below command:
$ java -cp bin Tomasulo arrayCopy.hex [-gui]
****************************************
simple test result:
After running the run.pl program the result is:
22 out of 22 passed.
The program passed all the tests.
also GUI works with the simulator
